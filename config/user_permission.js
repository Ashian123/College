let per = {};
per.admin = {
  edit_payment: true,
  view_payment: true,
};

per.staff = {
  edit_payment: false,
  edit_payment: false,
};

per.common = {
  edit_payment: true,
  edit_payment: true,
};

per.doctor = {
  edit_payment: true,
  edit_payment: true,
};

module.exports = per;